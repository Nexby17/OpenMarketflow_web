# PROGRESS.md — OpenMarketflow

Дневник работ. Правило (вшито 23.09): ПРИ КАЖДОМ коммите кода/конфигов — запись сюда + в feature_list.json в том же коммите или docs-коммитом немедленно после.

---

## 2026-09-23 — F-038: тейк-профит по реализуемому PnL (b82ecd0)

**Проблема:** арб GAZP/GZZ6 за два дня: 147 из 149 одиночных тейков profit_target закрылись В МИНУС (avg −17,13 ₽, суммарно −2 551,83 ₽); реализованный PnL −1 820,87 ₽. Корень: тейк считался по last-ценам (одиночные принты 1 лота неликвидного GZZ6 расходятся со стаканом на 2–40 ₽), а закрытие платило спред+slippage (фрикшн круга ≈15,5 ₽). «Мин. профит 25 ₽» — тейк-профит по коду (unrealized <= 0 → False), убыточный слой не закрывает; стоп фактически отсутствовал (risk_value 50 000 при капитале 30 000). update_market() существовал, но не вызывался — bid/ask в проверке всегда нули. Paper-режим пропускал проверку сессии → час 07:00: 130 сделок, −1 861 ₽.

**Решение Дмитрия:** входы не трогать (dev_ann_high работает); закрывать слоями как набирали (FIFO); доверять стакану; реализуемый PnL ≥ мин. профит (порог пользовательский); min_hold отклонён → подтверждение 3 тика.

**Реализовано:** _layer_realizable_pnl (bid/ask напрямую из ob_a/ob_b − slippage 5bps − комиссии; протухший >3с стакан блокирует решение); check_exit FIFO; подтверждение _EXIT_CONFIRM_TICKS=3; risk-exits без подтверждения; paper: EXIT ALL по стакану, лимит ноги A по стороне (SELL→bid). basis_calc остался на last — входная математика dev_ann не тронута. Тесты 42/42 (9 новых). .gitignore: arb_state_*.json.

**Реестр:** реестр фич отстал на 20 позиций (F-018..F-037 только в git-логе) — восстановлены из commit-сообщений; сегодняшний фикс зарегистрирован как F-038 (F-032 оказалась занята 637f27e). В meta.rules вшито: обновление реестра и дневников ОБЯЗАТЕЛЬНО при каждом коммите.

**Открытые вопросы (решает Дмитрий):** сессионная проверка в paper; risk_value 50 000 → 5 000?; фильтр деградированного стакана на входах; слой #192 (в минусе) — СТОП вручную или ждать.


Живой документ состояния проекта. Обновлять при каждом значимом изменении.
Формат записи: дата — что сделано — статус/доказательство.

---

## Текущее состояние (2026-08-19)

| Область | Статус |
|---|---|
| C# сервер :5050 | 🟢 Работает локально (REST-only Finam, логин admin) |
| Дашборд | 🟢 График (ChartController v80+), стакан, 3 робота в UI, счета реальные |
| finam_hub.py (WS-хаб) | 🟢 Протестирован: 1198 сообщений/60с, 0 ошибок |
| Paper-робот MXU6 :5181 | 🟡 Soak-обкатка PC-005 (новый стек: WS-hub + REST 4.3.3), монитористся ежечасно cron'ом |
| Боевые роботы :5070/5080/5081 | 🔴 Не запущены (ждут Этап 5 после PORT-вердикта) |
| Арбитраж :5090-5092 | 🔴 Не запущены |
| DataProvider :5060 | ⚪ Легаси, выводится из эксплуатации |
| Миграция на новое API | 🟡 Этапы 1–4 из 5 завершены (см. ниже) |

## Лента изменений

### 2026-08-27 — F-010: панели параметров работают без живого робота
- Репорт Дмитрия: «сохранить ничего не сохраняет». Корень: панель читала params только с живого /status; при остановленном роботе — пусто → фантазийные дефолты, а save уходил в мёртвый порт. Сохранение при живом роботе работало всегда (доказано: lots=3/step=99 от 25.08 целы).
- Фикс (bfc92d0): C#-прокси GET/POST /api/of/config/{of|of_mx} → robot/of_config*.json; панели читают конфиг при офлайне + бейдж источника (🟢/💾); save через робота ИЛИ прокси. app.js v83.
- Приёмка: API merge/rollback + CDP 5/5 PASS. Инцидент по ходу: тест перезаписал of_config_mx.json старыми значениями через checkout — актуальные значения восстановлены из веб-версии, предупреждение записано в уроки.

### 2026-08-25 — Автозапуск и режимы
- Автозапуск при загрузке Windows: scripts/autostart.ps1 + ярлык в Startup (004898b) — поднимает ТОЛЬКО сервер; торговые роботы убраны из автозапуска (d55c515) по решению Дмитрия: paper-режим только по явному указанию, роботы запускаются вручную.
- Конфиги сверены: боевой MX vs paper-lab — 3 отличия (maxAvg 15/150, accel 888/1900, bin 250/300), боевой верен.

### 2026-08-24–25 — Фичи Дмитрия: коррекция позиций + семантика кнопок
- POST /position/adjust на обоих OF-роботах (71992c2, 287f092): ручная синхронизация позиции с брокером без ордеров (lots со знаком + цена).
- UI «⚙ КОРРЕКЦИЯ ПОЗИЦИИ» в edit-панелях MX/Si (a67b0a0, v82): поля + подтверждение + результат; CDP-приёмка PASS.
- Семантика кнопок по ТЗ (f06e2e8): СТАРТ будит из паузы; СТОП закрывает все позиции; ПАУЗА ведёт открытые (стопы/ТП работают), новые запрещены — раньше paused замораживал всё (позиции без стоп-лосса!).
- /params: стабильный cfg-путь (не cwd) + try/except (битый JSON -> 400) — починено «параметры SiU6 не сохраняются».
- План-агент падал в обеих сессиях (перегруженная + новая) — работа выполнена main-агентом напрямую.

### 2026-08-21 — REAL-запуск MXU6 и его уроки
- Дмитрий одобрил real: запуск на счёте main 1225953. Вскрылись и починены в тот же день: Side-маппинг (SIDE_BUY/SELL), OrderType (ORDER_TYPE_MARKET/LIMIT) — все real-ордера падали на валидации; фантомный state (797K RT) — зомби-процессы перезаписывали чистку (4 коммита c4d2111..248249f); API переведён на threaded (поллинг UI больше не блокирует /stop).
- Дмитрий остановил робот после кривой (виртуальной) сделки: ордера до брокера НЕ дошли (ошибка валидации сохранила от шорта). Робот остановлен по команде, не включался.


### 2026-08-19
- Перезаписаны AGENTS.md / README.md под актуальный стек; создан PROGRESS.md (этот файл).

### 2026-08-18 — Миграция на новое Finam API (правило: одна фича → тест → ОК)
- **Этап 1** (f87a0b1): `finam_hub.py` — единый WS-коннект (wss://api.finam.ru/ws), JWT-автопродление, реконнект+backoff, outbox, fan-out. Тест: 60с — 1198 сообщений, 0 ошибок.
- **Этап 2 / PC-003** (819e96c): `hub_adapter.py` — мост WS→compat (обработчики робота не тронуты). Paper-робот MXU6 на хабе: OB/цена/сделки/бары из WS.
- **Этап 3 / PC-004** (3570399): `finam_rest4.py` — REST SDK 4.3.3 (py4-изоляция, patch_sdk: 2 бага SDK). Тест quote/account/quotas PASS.
- **Этап 4 / PC-005** (a25d89e): `orders_rest4.py` — paper-эмуляция/real-ордера; gRPC и DataProvider полностью удалены из paper-робота. Патч SDK №3 (timeout). Живой тест PASS.
- Soak-монитор PC-005: cron ежечасно, журнал в robot/paper_lab/papercuts.md.
- **Этап 5**: черновик ТЗ (STAGE5_DRAFT.md) — перенос на боевых роботов после PORT-вердикта.

### 2026-08-17 — UI и инфраструктура
- **ChartController** (goal 49649e5c ✅): полная реорганизация графика — modules/chart.js, единая точка истины, 0 ошибок `Value is null` (CDP-приёмка 14/14: 10 переключений TF, смена инструмента, live).
- Merge origin/main: VWEMA Avg Exit (B2), VAH/VAL range + breakout stop, /reset-stats, MX-робот UI (журнал/панель). Сохранены наши фиксы.
- Подключён реальный счёт: /api/accounts — equity FORTS 909 798 ₽ (актуально на 17.08).
- Контракты: SiM6→SiU6 везде (живые: SiU6/SiZ6/MXU6/MXZ6/GDU6/BRU6/RIU6).
- Стакан: реальный /api/orderbook (100 строк); фикс локали InvariantCulture (18 парсеров).
- REST-only FinamConnector (gRPC из C# удалён), автоподключение с FINAM_ACCOUNT_ID.

### 2026-08-13
- Сервер развёрнут локально, .env с боевым ключом, login admin (пароль по APP_PASS_HASH).
- Финамная миграция Python: finam_compat (shim FinamPy) для роботов.

### 2026-07-29 — Аудит и ремедиация (5 фаз)
- RiskGate, CircuitBreaker, MaxPositionRub; SignalR [Authorize], rate-limit, CORS;
  QUIK удалён; 22/22 тестов зелёные (17 C# + 5 Python).

## Метрики системы (для контекста)

- PSAR Grid MM: ~890K₽/мес на Si (историческая, прод-подтверждена)
- Капитал 10 млн ₽, цель +25% годовых net

## Открытые вопросы / следующие шаги

1. ⏳ Soak PC-005 ≥1 торговый день → вердикт PORT/DROP
2. ⏸ Этап 5: перенос на боевых роботов (после PORT; real-ордер минимальным лотом — только по явному одобрению)
3. ⏸ DataProvider: вывод из эксплуатации после Этапа 5
4. ✅ Позиции на боевом счёте — Дмитрия, не трогать (подтверждено 19.08)

## 2026-08-27 16:1x — F-011: видимая обратная связь кнопок (91b639b)

`ofRobotApi` не был определён — клики ▶⏸⏹ молча бросали ReferenceError. Добавлены async-обёртка + глобальные toast-уведомления (toastHost, верх-право); ошибки любого робота видны на любой вкладке. app.js v84. CDP-приёмка: клик по мёртвому роботу → toast + лог.

## 2026-08-27 16:3x — F-012: запуск робота одной кнопкой (352a466)

RobotProcessManager (C#): POST /api/of/process/{name}/{start|stop|status}; ▶ поднимает процесс python-робота, ждёт /health ≤25с, затем POST /start. Тосты прогресса. app.js v85. Живая приёмка: MX старт 17с → running; стоп → stopped, лоты 0.

## 2026-08-27 17:4x — F-013: PnL по фактическим ценам сделок (fix, df925e1)

**Инцидент.** SiU6 17:15: вход BUY 3, через 23с stop_loss_rub с «убытком» −42695р. Расследование по реальным сделкам брокера (account.get_trades): вход 86731 (1+2 лота), выход 86746 → реально ≈ +45р. Котировка в моменте 86726–86750.

**Корень.** `orders_rest4.wait_fill` при отсутствии `average_price` в ответе get_order падал на `limit_price` заявки (IOC-лимитка 92343/78113 — цена заявки, не исполнения). Стратегия получала фантомную цену входа → фантомный PnL → ложный стоп. Это же искажало realizedPnL/dailyPnL и историю сделок.

**Фикс (по фактическим сделкам):**
- `finam_rest4.get_today_trades(account_id)` — сделки счёта за UTC-день (GetTradesRequest start_time/end_time).
- `orders_rest4.wait_fill` (real): приоритет average_price → средневзвешенная цена сделок брокера по order_id → честный None; фолбэк на limit_price УДАЛЁН. Вызывающий код уже умеет «no broker fill — recording with strategy price» (WARNING), это безопаснее фантома.

**Тесты.** 4/4 PASS: (1) average_price приоритетна (86731, не 92343); (2) VWAP сделок 2+1 лота = 86732.0; (3) fill без цены → None, limit_price не просачивается; (4) paper-ветка цела. Интеграция: get_today_trades против живого брокера — 41 сделка, цены совпали с расследованием. py_compile обоих файлов OK.

**Коммит:** df925e1 (robot/orders_rest4.py, robot/finam_rest4.py).

## 2026-08-27 17:5x — F-014: статус SiU6 в UI (fix)

Строка Si (Order Flow, :5080) вечно показывала «🔴 Не запущен»: переменная ofRobot никогда не заполнялась — poll-функции не существовало (у MX была ofMxRobotPoll, у Si нет). Добавлен ofRobotPoll() + включён в общий 2с-цикл. app.js v86.

CDP-приёмка на живых роботах: «🟢 Работает» при running, «🔴 Остановлен» при stopped, скриншоты; node --check OK. Коммит (см. git log -1 --grep=F-014).
